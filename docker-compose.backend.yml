# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json
services:
  postgres:
    image: postgis/postgis:15-3.4
    container_name: antipoaching_postgres
    environment:
      POSTGRES_DB: anti-poaching-system
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d anti-poaching-system"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - antipoaching_network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: antipoaching_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - antipoaching_network
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: antipoaching_backend
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/anti-poaching-system
      REDIS_URL: redis://redis:6379
      SECRET_KEY: ${SECRET_KEY:-your-super-secret-key-change-in-production}
    volumes:
      - ./backend:/app
      - /app/venv
      - /app/__pycache__
      - uploads_data:/app/uploads
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    networks:
      - antipoaching_network
    restart: unless-stopped

networks:
  antipoaching_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  uploads_data: